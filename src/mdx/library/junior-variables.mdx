---
title: Variables
path: /library/junior-variables/
desc: Practice creating variables.
date: 2020-09-24
type: junior
tags: js
order: 2
difficulty: 1
---

import { Post, Text, CodeEditor, Link } from '~components';
import { getJs } from '~utils';

export const tasksListData = [
  <Text size="small" color="#FAFAFA">
    var firstBlood variable exists and has a value of 42
  </Text>,
  <Text size="small" color="#FAFAFA">
    let secondBlood variable exists and has a value of 42
  </Text>,
  <Text size="small" color="#FAFAFA">
    const thirdBlood variable exists and has a value of 42
  </Text>,
];

export const helpListData = [
  <Link size="small" href="https://www.w3schools.com/js/js_variables.asp">
    Variables
  </Link>,
  <Link size="small" href="https://www.w3schools.com/js/js_numbers.asp">
    Numbers
  </Link>,
];

export const tabs = ['js'];
export const tests = [
  () => getJs().firstBlood === 42,
  () => getJs().secondBlood === 42,
  () => getJs().thirdBlood === 42,
];

<Post>
  <CodeEditor
    tasks={tasksListData}
    help={helpListData}
    tabs={tabs}
    tests={tests}
    result={
      <>
        All three variables{' '}
        <Text size="preNormal" code>
          firstBlood
        </Text>
        , <Text size="preNormal" code>
          secondBlood
        </Text>, <Text size="preNormal" code>
          thirdBlood
        </Text> are declared and have a value of 42.
      </>
    }
    next="junior-hello-world"
  />
</Post>
