---
title: CSS Box Model
desc: Manage Margin, Border, Padding and a Content in CSS.
date: 2020-09-21
type: challenge
tags: html, css
difficulty: 1
---

import { Post, Text, CodeEditor, Link } from '~components';
import { getIframeContent } from '~utils';

export const tasksListData = [
  <Text size="small" color="#FAFAFA">
    div element exists
  </Text>,
  <Text size="small" color="#FAFAFA">
    "CSS Box Model" text is inside of the div element
  </Text>,
  <Text size="small" color="#FAFAFA">
    div has 20px margin
  </Text>,
  <Text size="small" color="#FAFAFA">
    div has 10px padding
  </Text>,
  <Text size="small" color="#FAFAFA">
    div has 1px dashed #000 border
  </Text>,
];

export const helpListData = [
  <Link size="small" href="https://www.w3schools.com/tags/tag_div.asp">
    Div
  </Link>,
  <Link size="small" href="https://www.w3schools.com/css/css_boxmodel.asp">
    CSS Box Model
  </Link>,
];

export const tabs = ['html', 'css'];
export const tests = [
  () => getIframeContent().find('div').length > 0,
  () =>
    getIframeContent().find('div').text().toLowerCase().trim() ===
    'css box model',
  () => getIframeContent().find('div').css('margin') === '20px',
  () => getIframeContent().find('div').css('padding') === '10px',
  () =>
    getIframeContent().find('div').css('border') === '1px dashed rgb(0, 0, 0)',
];

<Post>
  <CodeEditor
    tasks={tasksListData}
    help={helpListData}
    tabs={tabs}
    tests={tests}
    result={`"CSS Box Model" text is created using div element, has 20px margin, 10px padding, and 1px dashed #000 border.`}
    next="beginner-centering-elements"
  />
</Post>
