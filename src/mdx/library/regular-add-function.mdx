---
title: Add Function
path: /library/regular-add-function
desc: Create a function that adds two numbers and formats them to two decimals.
date: 2020-09-24
type: regular
tags: js
order: 3
difficulty: 2
---

import { Post, Text, CodeEditor, Link } from '~components';
import { getJs } from '~utils';

export const tasksListData = [
  <Text size="small" color="#FAFAFA">
    add function exists
  </Text>,
  <Text size="small" color="#FAFAFA">
    The add function accepts two numbers as parameters
  </Text>,
  <Text size="small" color="#FAFAFA">
    The add function formats addition to two decimals
  </Text>,
  <Text size="small" color="#FAFAFA">
    The add function returns formatted number as a string
  </Text>,
];

export const helpListData = [
  <Link
    size="small"
    href="https://stackoverflow.com/questions/3439040/why-does-adding-two-decimals-in-javascript-produce-a-wrong-result"
  >
    0.1 + 0.2
  </Link>,
  <Link
    size="small"
    href="https://www.w3schools.com/jsref/jsref_parsefloat.asp"
  >
    Parse Float
  </Link>,
  <Link size="small" href="https://www.w3schools.com/JSREF/jsref_tofixed.asp">
    To Fixed
  </Link>,
];

export const tabs = ['js'];
export const tests = [
  () => getJs().add,
  () => getJs().add?.length === 2,
  () => getJs().add?.(0.1, 0.2) === '0.30',
  () => getJs().add?.(0.5, 1) === '1.50',
];

<Post>
  <CodeEditor
    tasks={tasksListData}
    help={helpListData}
    tabs={tabs}
    tests={tests}
    result={
      <>
        <Text size="preNormal" code>
          add
        </Text>{' '}
        function accepts two numbers as parameters, adds them, properly formats
        to two decimals, and returns a formatted string.
      </>
    }
    next="regular-unique-function"
  />
</Post>
