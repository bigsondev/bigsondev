---
title: Palindrome Function
path: /library/senior-palindrome-function/
desc: Create a function that checks whether a string is a palindrome.
date: 2020-09-24
type: senior
tags: js
order: 2
difficulty: 3
---

import { Post, Text, CodeEditor, Link } from '~components';
import { getJs } from '~utils';

export const tasksListData = [
  <Text size="small" color="#FAFAFA">
    palindrome function exists
  </Text>,
  <Text size="small" color="#FAFAFA">
    The palindrome function accepts one string as a parameter
  </Text>,
  <Text size="small" color="#FAFAFA">
    When the provided string is a palindrome, the function returns true
  </Text>,
  <Text size="small" color="#FAFAFA">
    When the provided string isn't a palindrome, the function returns false
  </Text>,
];

export const helpListData = [
  <Link size="small" href="https://en.wikipedia.org/wiki/Palindrome">
    Palindrome
  </Link>,
];

export const tabs = ['js'];
export const tests = [
  () => getJs().palindrome,
  () => getJs().palindrome?.length === 1,
  () =>
    getJs().palindrome?.('eye') === true &&
    getJs().palindrome?.('kajak') === true,
  () =>
    getJs().palindrome?.('john') === false &&
    getJs().palindrome?.('doe') === false,
];

<Post>
  <CodeEditor
    tasks={tasksListData}
    help={helpListData}
    tabs={tabs}
    tests={tests}
    result={
      <>
        <Text size="preNormal" code>
          palindrome
        </Text>{' '}
        function accepts one string as a parameter and returns true when the
        provided string is a palindrome.
      </>
    }
  />
</Post>
