---
title: Button Event
path: /library/junior-button-event
desc: Practice creating onclick Event on button.
date: 2020-09-24
type: junior
tags: js
order: 7
difficulty: 2
---

import { Post, Text, CodeEditor, Link } from '~components';
import { getJs, getIframeContent } from '~utils';

export const tasksListData = [
  <Text size="small" color="#FAFAFA">
    button element exists
  </Text>,
  <Text size="small" color="#FAFAFA">
    "Click Me" text is inside of the button element
  </Text>,
  <Text size="small" color="#FAFAFA">
    The button element has onclick attribute with popup() function
  </Text>,
  <Text size="small" color="#FAFAFA">
    popup function exists and optionally has an alert('Hi') which shows after
    clicking on the button
  </Text>,
];

export const helpListData = [
  <Link size="small" href="https://www.w3schools.com/tags/tag_button.asp">
    Button
  </Link>,
  <Link size="small" href="https://www.w3schools.com/jsref/event_onclick.asp">
    Onclick Event
  </Link>,
  <Link size="small" href="https://www.w3schools.com/jsref/met_win_alert.asp">
    Alert
  </Link>,
];

export const tabs = ['html', 'js'];
export const tests = [
  () => getIframeContent().find('button').length === 1,
  () =>
    getIframeContent().find('button').text().trim().toLowerCase() ===
    'click me',
  () => getIframeContent().find('button').attr('onclick') === 'popup()',
  () => getJs().popup,
];

<Post>
  <CodeEditor
    tasks={tasksListData}
    help={helpListData}
    tabs={tabs}
    tests={tests}
    result={`"Click Me" button is displayed on the screen and alerts "Hi" after clicking on it.`}
  />
</Post>
