---
title: Hello World
path: /library/junior-hello-world/
desc: Create a function that says "Hello World".
date: 2020-09-24
type: junior
tags: js
order: 3
difficulty: 1
---

import { Post, Text, CodeEditor, Link } from '~components';
import { getJs } from '~utils';

export const tasksListData = [
  <Text size="small" color="#FAFAFA">
    hello function exists
  </Text>,
  <Text size="small" color="#FAFAFA">
    The hello function returns "Hello World" string
  </Text>,
];

export const helpListData = [
  <Link
    size="small"
    href="https://www.w3schools.com/jsref/jsref_obj_string.asp"
  >
    Strings
  </Link>,
  <Link size="small" href="https://www.w3schools.com/js/js_functions.asp">
    Functions
  </Link>,
];

export const tabs = ['js'];
export const tests = [
  () => getJs().hello,
  () => getJs().hello?.()?.toLowerCase() === 'hello world',
];

<Post>
  <CodeEditor
    tasks={tasksListData}
    help={helpListData}
    tabs={tabs}
    tests={tests}
    result={
      <>
        <Text size="preNormal" code>
          hello
        </Text>{' '}
        function returns a "Hello World" string.
      </>
    }
    next="junior-arrays"
  />
</Post>
