---
title: Inline Elements
path: /library/beginner-inline-elements/
desc: Create your first inline element.
date: 2020-09-21
type: beginner
tags: html
order: 3
difficulty: 0
---

import { Post, Text, CodeEditor, Link } from '~components';
import { getIframeContent } from '~utils';

export const tasksListData = [
  <Text size="small" color="#FAFAFA">
    span element exists
  </Text>,
  <Text size="small" color="#FAFAFA">
    "Hello Inline" text is inside of the span element
  </Text>,
];

export const helpListData = [
  <Link size="small" href="https://www.w3schools.com/html/html_blocks.asp">
    Block and Inline Elements
  </Link>,
];

export const tabs = ['html'];
export const tests = [
  () => getIframeContent().find('span').length > 0,
  () =>
    getIframeContent().find('span').text().toLowerCase().trim() ===
    'hello inline',
];

<Post>
  <CodeEditor
    tasks={tasksListData}
    help={helpListData}
    tabs={tabs}
    tests={tests}
    result={`"Hello Inline" span is displayed on the screen.`}
    next="beginner-comments"
/>


</Post>

