---
title: Arrays
desc: Practice creating arrays.
date: 2020-09-24
type: challenge
tags: js
difficulty: 1
---

import { Post, Text, CodeEditor, Link } from '~components';
import { getJs } from '~utils';

export const tasksListData = [
  <Text size="small" color="#FAFAFA">
    friends array exists
  </Text>,
  <Text size="small" color="#FAFAFA">
    The friends array has 3 items which are the following strings - "John",
    "Jane", "Joe" (in that order)
  </Text>,
];

export const helpListData = [
  <Link size="small" href="https://www.w3schools.com/js/js_arrays.asp">
    Arrays
  </Link>,
];

export const tabs = ['js'];
export const tests = [
  () => getJs().friends,
  () => {
    const friends = getJs().friends;
    const expectedResult = ['John', 'Jane', 'Joe'];
    return (
      friends?.length === 3 &&
      friends.every((value, index) => value === expectedResult[index])
    );
  },
];

<Post>
  <CodeEditor
    tasks={tasksListData}
    help={helpListData}
    tabs={tabs}
    tests={tests}
    result={
      <>
        <Text code size="preNormal">
          friends
        </Text>{' '}
        array is declared and has 3 items in it which are "John", "Jane", "Joe"
        strings.
      </>
    }
    next="junior-objects"
  />
</Post>
