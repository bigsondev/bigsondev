---
title: Centering Elements
desc: Practice controlling the position of elements through Flexbox.
date: 2020-09-21
type: challenge
tags: html, css
difficulty: 2
---

import $ from 'cash-dom';

import { Post, Text, CodeEditor, Link } from '~components';
import { getIframeContent } from '~utils';

export const tasksListData = [
  <Text size="small" color="#FAFAFA">
    div element exists
  </Text>,
  <Text size="small" color="#FAFAFA">
    p element exists
  </Text>,
  <Text size="small" color="#FAFAFA">
    p is inside of the div element
  </Text>,
  <Text size="small" color="#FAFAFA">
    div has a display set to flex
  </Text>,
  <Text size="small" color="#FAFAFA">
    div has a justify-content set to center
  </Text>,
  <Text size="small" color="#FAFAFA">
    div has an align-items set to center
  </Text>,
  <Text size="small" color="#FAFAFA">
    p has 200px width
  </Text>,
  <Text size="small" color="#FAFAFA">
    p has 200px height
  </Text>,
  <Text size="small" color="#FAFAFA">
    p has a blue background-color
  </Text>,
];

export const helpListData = [
  <Link size="small" href="https://www.w3schools.com/css/css3_flexbox.asp">
    Flexbox
  </Link>,
];

export const tabs = ['html', 'css'];
export const tests = [
  () => getIframeContent().find('div').length > 0,
  () => getIframeContent().find('p').length > 0,
  () => getIframeContent().find('div p').length > 0,
  () => getIframeContent().find('div').css('display') === 'flex',
  () => getIframeContent().find('div').css('justify-content') === 'center',
  () => getIframeContent().find('div').css('align-items') === 'center',
  () => getIframeContent().find('p').css('width') === '200px',
  () => getIframeContent().find('p').css('height') === '200px',
  () =>
    getIframeContent().find('p').css('background-color') === 'rgb(0, 0, 255)',
];

<Post>
  <CodeEditor
    tasks={tasksListData}
    help={helpListData}
    tabs={tabs}
    tests={tests}
    result={`The p element has 200x200 dimensions, a blue background-color, and is absolutely centered on the screen through flex div container.`}
  />
</Post>
