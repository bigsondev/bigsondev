---
title: Multiply Function
desc: Create a function to multiply two numbers.
date: 2020-09-24
type: challenge
tags: js
difficulty: 2
---

import { Post, Text, CodeEditor, Link } from '~components';
import { getJs } from '~utils';

export const tasksListData = [
  <Text size="small" color="#FAFAFA">
    multiply function exists
  </Text>,
  <Text size="small" color="#FAFAFA">
    The multiply function accepts two numbers as parameters
  </Text>,
  <Text size="small" color="#FAFAFA">
    The multiply function returns the result of two numbers multiplication
  </Text>,
];

export const helpListData = [
  <Link
    size="small"
    href="https://www.w3schools.com/js/js_function_parameters.asp"
  >
    Function Parameters
  </Link>,
];

export const tabs = ['js'];
export const tests = [
  () => getJs().multiply,
  () => getJs().multiply?.length === 2,
  () =>
    getJs().multiply?.(5, 5) === 25 &&
    getJs().multiply?.(1, 1) === 1 &&
    getJs().multiply?.(0, 0) === 0 &&
    getJs().multiply?.(1, 2) === 2 &&
    getJs().multiply?.(2, 1) === 2 &&
    getJs().multiply?.(7, 7) === 49,
];

<Post>
  <CodeEditor
    tasks={tasksListData}
    help={helpListData}
    tabs={tabs}
    tests={tests}
    result={
      <>
        <Text size="preNormal" code>
          multiply
        </Text>{' '}
        function accepts two numbers as parameters and properly multiplies
        numbers.
      </>
    }
    next="junior-button-event"
  />
</Post>
