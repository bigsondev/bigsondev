---
title: Beautiful Heading
path: /library/beginner-beautiful-heading
desc: Let's practice how to style elements using CSS.
date: 2020-09-17
type: beginner
tags: html, css
order: 3
difficulty: 1
---

import { Post, Text, CodeEditor, Link } from '~components';
import { getIframeContent } from '~utils';

export const tasksListData = [
  <Text size="small" color="#FAFAFA">
    h1 element exists
  </Text>,
  <Text size="small" color="#FAFAFA">
    Beautiful Heading text is inside of the h1 element
  </Text>,
  <Text size="small" color="#FAFAFA">
    blue color is applied to the h1 element
  </Text>,
  <Text size="small" color="#FAFAFA">
    h1 element is centered on the site
  </Text>,
];

export const helpListData = [
  <Link size="small" href="https://www.w3schools.com/tags/tag_hn.asp">
    Headings
  </Link>,
  <Link size="small" href="https://www.w3schools.com/cssref/pr_text_color.asp">
    Colors
  </Link>,
  <Link
    size="small"
    href="https://www.w3schools.com/cssref/pr_text_text-align.ASP"
  >
    Text Align
  </Link>,
];

export const tabs = ['html', 'css'];
export const tests = [
  () => getIframeContent().find('h1').length > 0,
  () =>
    getIframeContent().find('h1').text().toLowerCase().trim() ===
    'beautiful heading',
  () => getIframeContent().find('h1').css('color') === 'rgb(0, 0, 255)',
  () => getIframeContent().find('h1').css('text-align') === 'center',
];

<Post>
  <CodeEditor
    tasks={tasksListData}
    help={helpListData}
    tabs={tabs}
    tests={tests}
    result={`The "Beautiful Heading" text is created using h1 element, has blue color and is centered on the screen.`}
  />
</Post>
