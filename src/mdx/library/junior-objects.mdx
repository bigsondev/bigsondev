---
title: Objects
path: /library/junior-objects/
desc: Practice creating objects.
date: 2020-09-24
type: junior
tags: js
order: 5
difficulty: 1
---

import { Post, Text, CodeEditor, Link } from '~components';
import { getJs } from '~utils';

export const tasksListData = [
  <Text size="small" color="#FAFAFA">
    dog object exists
  </Text>,
  <Text size="small" color="#FAFAFA">
    The dog object has 3 properties
  </Text>,
  <Text size="small" color="#FAFAFA">
    The dog object has non-empty name property
  </Text>,
  <Text size="small" color="#FAFAFA">
    The dog object has non-empty age property
  </Text>,
  <Text size="small" color="#FAFAFA">
    The dog object has non-empty breed property
  </Text>,
];

export const helpListData = [
  <Link size="small" href="https://www.w3schools.com/js/js_objects.asp">
    Objects
  </Link>,
];

export const tabs = ['js'];
export const tests = [
  () => getJs().dog,
  () => Object.keys(getJs().dog || {}).length === 3,
  () => Boolean(getJs().dog?.name),
  () => Boolean(getJs().dog?.age),
  () => Boolean(getJs().dog?.breed),
];

<Post>
  <CodeEditor
    tasks={tasksListData}
    help={helpListData}
    tabs={tabs}
    tests={tests}
    result={
      <>
        <Text code size="preNormal">
          dog
        </Text>{' '}
        object is declared and has 3 properties in it which are name, age,
        breed. All of them are non-empty.
      </>
    }
    next="junior-multiply-function"
  />
</Post>
