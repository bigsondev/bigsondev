---
title: Hello World
path: /library/beginner-hello-world
desc: The very first challenge for all aspiring Frontend Developers.
date: 2020-09-15
type: beginner
tags: html
order: 2
difficulty: 0
---

import { Post, Text, CodeEditor, Link } from '~components';
import { getIframeContent } from '~utils';

export const tasksListData = [
  <Text size="small" color="#FAFAFA">
    p element exists
  </Text>,
  <Text size="small" color="#FAFAFA">
    Hello World text is inside of the p element
  </Text>,
];

export const helpListData = [
  <Link size="small" href="https://www.w3schools.com/html/html_paragraphs.asp">
    Paragraphs
  </Link>,
];

export const tabs = ['html'];
export const tests = [
  () => getIframeContent().find('p').length > 0,
  () =>
    getIframeContent().find('p').text().toLowerCase().trim() === 'hello world',
];

<Post>
  <CodeEditor
    tasks={tasksListData}
    help={helpListData}
    tabs={tabs}
    tests={tests}
    result={`The "Hello World" paragraph is displayed on the screen.`}
  />
</Post>
