---
title: Sum using reduce
desc: Use the versatile .reduce method to sum an array of numbers.
date: 2020-12-14
tags: js
type: boy
image: pillSix
---

import {
  SyntaxHighlighter,
  Link,
  CodeEditorRaw,
  KnowledgePill,
  Text,
} from '~components';

export const reduceCode = `const numbers = [5, 10, 15, 20];
const sum = numbers.reduce((acc, value) => acc += value, 0);`;
export const initialHtml = '<h1>Open console in your browser to see logs</h1>';
export const initialJs = `const numbers = [5, 10, 15, 20];
const sum = numbers.reduce((acc, value) => acc += value, 0);\n
console.log(sum); // 50`;

export const tabs = ['js', 'html'];

<KnowledgePill
  code={<SyntaxHighlighter showCopy>{reduceCode}</SyntaxHighlighter>}
  explanation={
    <>
      <Link
        href="https://www.w3schools.com/jsref/jsref_reduce.asp"
        size="preNormal"
      >
        Reduce method
      </Link>{' '}
      is very flexible and can help in all kinds of arrays transformations. It
      can reduce numbers to sum them up or calculate the average number for
      example. It also works great with other data structures such as arrays of
      strings or arrays of objects.
    </>
  }
  interactiveExample={
    <CodeEditorRaw
      tabs={tabs}
      initialHtml={initialHtml}
      initialJs={initialJs}
    />
  }
  {...props}
  {...props.frontmatter}
/>
