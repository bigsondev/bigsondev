---
title: Sum using reduce
desc: Use the versatile .reduce method to sum an array of numbers.
date: 2020-12-14
image: none
imageSource: none
imageSourceLink: none
author: none
authorLink: none
tags: js
difficulty: 2
---

import {
  SyntaxHighlighter,
  Link,
  KnowledgePill,
  Text,
  Title,
  PostList,
} from '~components';

export const reduceCode = `const numbers = [5, 10, 15, 20];
const sum = numbers.reduce((acc, value) => acc += value, 0);`;

export const listData = [
  <Text>
    <Link href="https://www.w3schools.com/jsref/jsref_reduce.asp">
      Reduce method
    </Link>{' '}
    is very flexible and can help in all kinds of arrays transformations.{' '}
  </Text>,
  <Text>
    It can reduce numbers to sum them up or calculate the average number for
    example.{' '}
  </Text>,
  <Text>
    It also works great with other data structures such as arrays of strings or
    arrays of objects.
  </Text>,
];

<KnowledgePill {...props} {...props.frontmatter}>
  <Title level={4} transform="none">
    Sum those numbers
  </Title>
  <SyntaxHighlighter>{reduceCode}</SyntaxHighlighter>
  <Title level={4} transform="none">
    Explanation
  </Title>
  <PostList data={listData} />
</KnowledgePill>
