---
title: Add, edit, delete operations
desc: Learn how to add, edit and delete array of objects in semantic, declarative approach using rest spread operator and array methods.
date: 2020-12-05
image: add-edit-delete-array-of-objects.jpg
imageSource: Pexels
imageSourceLink: https://www.pexels.com/
author: George Becker
authorLink: https://www.pexels.com/pl-pl/zdjecie/1-1-3-tekst-na-czarnej-tablicy-374918/
tags: js
difficulty: 3
---

import {
  SyntaxHighlighter,
  Link,
  KnowledgePill,
  Text,
  Title,
  PostList,
} from '~components';

export const jsCode = `let items = [];\n
// Add item to the beginning of items array using rest operator
const addItemToStart = (newItem) => {
  items = [newItem, ...items];
}\n
// Add item to the end of items array using rest operator
const addItemToEnd = (newItem) => {
  items = [...items, newItem];
}\n
// Edit item using .map method, if id matches, use rest operator to override
// item properties with updatedItem, otherwise return item as is
const editItem = (updatedItem) => {
  items = items.map((item) => {
    if (updatedItem.id === item.id) {
      return {
        ...item,
        ...updatedItem
      }
    }\n
    return item;
  })
}\n
// Delete item using .filter method, if id matches, delete it from items array
const deleteItem = (idToRemove) => {
  items = items.filter((item) => item.id !== idToRemove);
}
`;

export const listData = [
  <Text>
    <Link href="https://javascript.info/rest-parameters-spread">
      Rest spread operator
    </Link>{' '}
    is helpful when it comes to working with arrays and objects.
  </Text>,
  <Text>
    Add, edit, delete functions can be used on any object structure and should
    work well with the array of those objects.
  </Text>,
];

<KnowledgePill {...props} {...props.frontmatter}>
  <Title level={4} transform="none">
    Immutability with rest spread operator
  </Title>
  <Text size="small">JavaScript</Text>
  <SyntaxHighlighter language="javascript" showCopy>
    {jsCode}
  </SyntaxHighlighter>
  <Title level={4} transform="none">
    Summary
  </Title>
  <PostList data={listData} />
</KnowledgePill>
