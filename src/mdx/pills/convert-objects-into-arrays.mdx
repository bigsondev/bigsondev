---
title: Convert data structures
desc: Converting data structures is a day-to-day job as a Developer - so is staying up-to-date with the language and it's features.
date: 2020-12-17
image: none
imageSource: none
imageSourceLink: none
author: none
authorLink: none
tags: js
difficulty: 2
---

import {
  SyntaxHighlighter,
  Link,
  KnowledgePill,
  Text,
  Title,
  PostList,
} from '~components';

export const codeSnippet = `const obj = {
  a: 1,
  b: 2,
  c: 3
};\n
// All of the below are now an array,
// you can use all the array methods and properties, 
// e.g. .reduce or .length
const keys = Object.keys(obj); // ['a', 'b', 'c']
const values = Object.values(obj); // [1, 2, 3]
const entries = Object.entries(obj); // [["a", 1], ["b", 2], ["c", 3]]`;

export const listData = [
  <Text>
    <Link href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/keys">
      Object.keys()
    </Link>
    , <Link href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/values">
      Object.values()
    </Link>, <Link href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/entries">
      Object.entries()
    </Link>.
  </Text>,
  <Text>
    The above are ES8 (ECMAScript 2017) new methods which are extremely helpful
    when converting from an <Text code>Object</Text> into an{' '}
    <Text code>Array</Text>.
  </Text>,
];

<KnowledgePill {...props} {...props.frontmatter}>
  <Title level={4} transform="none">
    From object to array
  </Title>
  <SyntaxHighlighter>{codeSnippet}</SyntaxHighlighter>
  <Title level={4} transform="none">
    Explanation
  </Title>
  <PostList data={listData} />
</KnowledgePill>
