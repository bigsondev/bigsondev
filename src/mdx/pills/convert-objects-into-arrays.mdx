---
path: /pills/convert-objects-into-arrays/
desc: Converting data structures is a day-to-day job as a Developer - so is staying up-to-date with the language and it's features.
date: 2020-12-17
tags: js
type: boy
image: pillEight
---

import { Link as GatsbyLink } from 'gatsby';
import { Row, Col } from 'antd';

import {
  theme,
  Title,
  Paragraph,
  Text,
  Illustration,
  SyntaxHighlighter,
  MainCard,
  Image,
  Button,
  Link,
  CodeEditorRaw,
} from '~components';

export const codeSnippet = `const obj = {
  a: 1,
  b: 2,
  c: 3
};\n
// All of the below are now an array,
// you can use all the array methods and properties, 
// e.g. .reduce or .length
const keys = Object.keys(obj); // ['a', 'b', 'c']
const values = Object.values(obj); // [1, 2, 3]
const entries = Object.entries(obj); // [["a", 1], ["b", 2], ["c", 3]]`;
export const initialHtml = '<h1>Open console in your browser to see logs</h1>';
export const initialJs = `const obj = {
  a: 1,
  b: 2,
  c: 3
};\n
// All of the below are now an array,
// you can use all the array methods and properties, 
// e.g. .reduce or .length
const keys = Object.keys(obj);
const values = Object.values(obj);
const entries = Object.entries(obj);\n
console.log(keys); // ['a', 'b', 'c']
console.log(values); // [1, 2, 3]
console.log(entries); // [["a", 1], ["b", 2], ["c", 3]]`;

export const tabs = ['js', 'html'];

<Row gutter={[0, 24]}>
  <Col span={24}>
    <MainCard>
      <Row justify="center">
        <Col span={24}>
          <Title
            level={3}
            transform="none"
            align="center"
            marginBottom="0"
            fontSize={theme.fontSize.superLarge}
            fontWeight={theme.fontWeight.stronger}
          >
            <Row justify="center" align="center" gutter={16}>
              <Col>Knowledge Pill</Col>
              <Col>
                <Illustration
                  type={
                    props.pageContext.frontmatter.type === 'girl'
                      ? 'girlPill'
                      : 'boyPill'
                  }
                  width={48}
                  style={{ marginTop: -6 }}
                />
              </Col>
            </Row>
          </Title>
        </Col>
        <Col span={24} sm={22}>
          <Paragraph align="center">
            {props.pageContext.frontmatter.desc}
          </Paragraph>
        </Col>
      </Row>
    </MainCard>
  </Col>
  <Col span={24}>
    <MainCard>
      <Row justify="center">
        <Col span={24}>
          <Title
            level={3}
            transform="none"
            align="center"
            fontSize={theme.fontSize.superLarge}
            fontWeight={theme.fontWeight.stronger}
          >
            Code
          </Title>
        </Col>
        <Col span={24}>
          <SyntaxHighlighter showCopy>{codeSnippet}</SyntaxHighlighter>
        </Col>
        <Col span={24}>
          <Title level={4} transform="none" align="center">
            Explanation
          </Title>
        </Col>
        <Col xs={24} sm={22}>
          <Paragraph align="center" size="preNormal" fontStyle="italic">
            <Link
              href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/keys"
              size="preNormal"
            >
              Object.keys()
            </Link>
            , <Link
              href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/values"
              size="preNormal"
            >
              Object.values()
            </Link>, <Link
              href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/entries"
              size="preNormal"
            >
              Object.entries()
            </Link> are ES8 (ECMAScript 2017) new methods which are extremely helpful
            when converting from an <Text code>Object</Text> into an <Text code>
              Array
            </Text>.
          </Paragraph>
        </Col>
      </Row>
    </MainCard>
  </Col>
  <Col span={24}>
    <MainCard>
      <Row justify="center">
        <Col span={24}>
          <Title
            level={3}
            transform="none"
            align="center"
            fontSize={theme.fontSize.superLarge}
            fontWeight={theme.fontWeight.stronger}
          >
            Interactive example
          </Title>
        </Col>
        <Col span={24}>
          <CodeEditorRaw
            tabs={tabs}
            initialHtml={initialHtml}
            initialJs={initialJs}
          />
        </Col>
      </Row>
    </MainCard>
  </Col>
</Row>
