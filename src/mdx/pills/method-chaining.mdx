---
path: /pills/method-chaining/
desc: Method chaining is a helpful pattern to learn in programming. It complies with the functional, declarative approach and reduces repetition.
date: 2020-12-15
tags: js
type: boy
image: pillSeven
---

import { Link as GatsbyLink } from 'gatsby';
import { Row, Col } from 'antd';

import {
  theme,
  Title,
  Paragraph,
  Illustration,
  SyntaxHighlighter,
  MainCard,
  Image,
  Button,
  Link,
  CodeEditorRaw,
} from '~components';

export const methodChainingCode = `// Let's count vowels in a string using the method chaining
const VOWELS = ['a', 'e', 'i', 'o', 'u'];\n
// Reduce an array of letters to get the sum of vowels
// and pass a function to .reduce method to make it more readable
const countVowels = (sum, letter) => (VOWELS.includes(letter) ? ++sum : sum);\n
// 1. .replace returns a string without whitespaces
// 2. .split returns an array of letters
// 3. .reduce reduces an array of letters into a single number - the sum of vowels
const getVowelsCount = (string) =>
  string.replace(/ /g, '').split('').reduce(countVowels, 0);
`;
export const initialHtml = '<h1>Open console in your browser to see logs</h1>';
export const initialJs = `// Let's count vowels in a string using the method chaining
const VOWELS = ['a', 'e', 'i', 'o', 'u'];\n
// Reduce an array of letters to get the sum of vowels
// and pass a function to .reduce method to make it more readable
const countVowels = (sum, letter) => (VOWELS.includes(letter) ? ++sum : sum);\n
// 1. .replace returns a string without whitespaces
// 2. .split returns an array of letters
// 3. .reduce reduces an array of letters into a single number - the sum of vowels
const getVowelsCount = (string) =>
  string.replace(/ /g, '').split('').reduce(countVowels, 0);\n
console.log(getVowelsCount('Hello World!')); // 3
console.log(getVowelsCount('bbb')); // 0
console.log(getVowelsCount('a b c d e')); // 2  
`;

export const tabs = ['js', 'html'];

<Row gutter={[0, 24]}>
  <Col span={24}>
    <MainCard>
      <Row justify="center">
        <Col span={24}>
          <Title
            level={3}
            transform="none"
            align="center"
            marginBottom="0"
            fontSize={theme.fontSize.superLarge}
            fontWeight={theme.fontWeight.stronger}
          >
            <Row justify="center" align="center" gutter={16}>
              <Col>Knowledge Pill</Col>
              <Col>
                <Illustration
                  type={
                    props.pageContext.frontmatter.type === 'girl'
                      ? 'girlPill'
                      : 'boyPill'
                  }
                  width={48}
                  style={{ marginTop: -6 }}
                />
              </Col>
            </Row>
          </Title>
        </Col>
        <Col span={24} sm={22}>
          <Paragraph align="center">
            {props.pageContext.frontmatter.desc}
          </Paragraph>
        </Col>
      </Row>
    </MainCard>
  </Col>
  <Col span={24}>
    <MainCard>
      <Row justify="center">
        <Col span={24}>
          <Title
            level={3}
            transform="none"
            align="center"
            fontSize={theme.fontSize.superLarge}
            fontWeight={theme.fontWeight.stronger}
          >
            Code
          </Title>
        </Col>
        <Col span={24}>
          <SyntaxHighlighter showCopy>{methodChainingCode}</SyntaxHighlighter>
        </Col>
        <Col span={24}>
          <Title level={4} transform="none" align="center">
            Explanation
          </Title>
        </Col>
        <Col xs={24} sm={22}>
          <Paragraph align="center" size="preNormal" fontStyle="italic">
            <Link
              href="https://www.tutorialspoint.com/method-chaining-in-javascript"
              size="preNormal"
            >
              Method chaining
            </Link>{' '}
            is a really good pattern for avoiding repetition and making your
            code more readable. It plays well with functional programming and is
            often used in JavaScript. Each of the methods needs to return
            something in order to call another method on it. Hence the chaining
            name.
          </Paragraph>
        </Col>
      </Row>
    </MainCard>
  </Col>
  <Col span={24}>
    <MainCard>
      <Row justify="center">
        <Col span={24}>
          <Title
            level={3}
            transform="none"
            align="center"
            fontSize={theme.fontSize.superLarge}
            fontWeight={theme.fontWeight.stronger}
          >
            Interactive example
          </Title>
        </Col>
        <Col span={24}>
          <CodeEditorRaw
            tabs={tabs}
            initialHtml={initialHtml}
            initialJs={initialJs}
          />
        </Col>
      </Row>
    </MainCard>
  </Col>
</Row>
