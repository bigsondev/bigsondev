---
path: /pills/outline/
desc: Outline all your HTML elements with a single line of CSS. Helps with understanding layout by visualizing all elements outlines on the screen.
date: 2020-11-30
tags: html, css
type: boy
image: pillOne
---

import { Link as GatsbyLink } from 'gatsby';
import { Row, Col } from 'antd';

import { PillOne } from '~assets';
import {
  theme,
  Title,
  Paragraph,
  Illustration,
  SyntaxHighlighter,
  MainCard,
  Image,
  Button,
  Link,
  CodeEditorRaw,
} from '~components';

export const outlineCode = `* {
  outline: 1px solid red;  
}`;

export const tabs = ['html', 'css'];
export const initialHtml = `<div>Hello there, I'm a block element.</div>
<span>Hello div, I'm inline.</span>`;
export const initialCss = `* {
  outline: 1px solid red;
}\n
body {
  height: 100vh;
}\n
div {
  margin-bottom: 1rem;
}`;

<Row gutter={[0, 24]}>
  <Col span={24}>
    <MainCard>
      <Row justify="center">
        <Col span={24}>
          <Title
            level={3}
            transform="none"
            align="center"
            marginBottom="0"
            fontSize={theme.fontSize.superLarge}
            fontWeight={theme.fontWeight.stronger}
          >
            <Row justify="center" align="center" gutter={16}>
              <Col>Knowledge Pill</Col>
              <Col>
                <Illustration
                  type={
                    props.pageContext.frontmatter.type === 'girl'
                      ? 'girlPill'
                      : 'boyPill'
                  }
                  width={48}
                  style={{ marginTop: -6 }}
                />
              </Col>
            </Row>
          </Title>
        </Col>
        <Col span={24} sm={22}>
          <Paragraph align="center">
            {props.pageContext.frontmatter.desc}
          </Paragraph>
        </Col>
      </Row>
    </MainCard>
  </Col>
  <Col span={24}>
    <MainCard>
      <Row justify="center">
        <Col span={24}>
          <Title
            level={3}
            transform="none"
            align="center"
            fontSize={theme.fontSize.superLarge}
            fontWeight={theme.fontWeight.stronger}
          >
            Code
          </Title>
        </Col>
        <Col span={24}>
          <SyntaxHighlighter language="css" showCopy>
            {outlineCode}
          </SyntaxHighlighter>
        </Col>
        <Col span={24}>
          <Title level={4} transform="none" align="center">
            Explanation
          </Title>
        </Col>
        <Col xs={24} sm={22}>
          <Paragraph align="center" size="preNormal" fontStyle="italic">
            <Link
              href="https://www.w3schools.com/cssref/sel_all.asp"
              size="preNormal"
            >
              Select all selector
            </Link>{' '}
            targets all HTML elements and adds{' '}
            <Link
              href="https://www.w3schools.com/css/css_outline.asp"
              size="preNormal"
            >
              outline
            </Link>{' '}
            to them.
          </Paragraph>
        </Col>
      </Row>
    </MainCard>
  </Col>
  <Col span={24}>
    <MainCard>
      <Row justify="center">
        <Col span={24}>
          <Title
            level={3}
            transform="none"
            align="center"
            fontSize={theme.fontSize.superLarge}
            fontWeight={theme.fontWeight.stronger}
          >
            Interactive example
          </Title>
        </Col>
        <Col span={24}>
          <CodeEditorRaw
            tabs={tabs}
            initialHtml={initialHtml}
            initialCss={initialCss}
          />
        </Col>
      </Row>
    </MainCard>
  </Col>
</Row>
