---
title: Crispy features
desc: Null coalescing operator, optional chaining, and Array.flat method are all amazing. It's worth to follow ESNext features to be up-to-date with new possibilities.
date: 2021-01-12
image: none
imageSource: none
imageSourceLink: none
author: none
authorLink: none
tags: js
difficulty: 2
---

import {
  SyntaxHighlighter,
  Link,
  KnowledgePill,
  Text,
  Title,
  PostList,
} from '~components';

export const codeSnippet = `const data = {};
const person = {
  name: 'Adrian',
  age: 27
};
const animals = ['Dog', 'Cat'];
const nestedAnimals = [['Dog'], ['Cat'], ['Horse']];\n
// Thanks to the "Elvis operator" or more known as optional chaining,
// we can safely access nested object properties even if they don't exist
console.log(data?.a?.b?.c); // undefined instead of error\n
// We can do the similar with arrays, since arrays are indexed from 0,
// animals[2] is the 3rd element which doesn't exist
console.log(animals[2]?.toString()); // undefined instead of error\n
// We can also use optional chaining on functions, in this case
// we are trying to call "myImaginaryFunction" - which again, doesn't exist
console.log(data.myImaginaryFunction?.()); // undefined instead of error\n
// We can still use optional chaining to access nested properties
console.log(person?.name); // "Adrian"\n
// Null coalescing operator is best suited for checking against null or undefined
// It will accept other "falsy" values such as 0
const zero = 0 ?? 5;
const five = null ?? 5;
const ten = undefined ?? 10;\n
console.log(zero, five, ten); // 0, 5, 10\n
// .flat creates a flatten array, it accepts depth as an optional
// parameter of how many levels it should flatten the original array
// by default it's 1 level
const flattenAnimals = nestedAnimals.flat();\n
console.log(flattenAnimals); // ["Dog", "Cat", "Horse"]`;

export const listData = [
  <Text>
    <Link href="https://en.wikipedia.org/wiki/ECMAScript">ECMAScript</Link>{' '}
    brings up new features every year to the JavaScript world.{' '}
  </Text>,
  <Text>It's challenging to stay up-to-date but also rewarding.</Text>,
  <Text>
    {' '}
    <Link href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Optional_chaining">
      Optional chaining
    </Link> is a true game-changer for writing code.{' '}
  </Text>,
  <Text>
    If it comes to arrays, worth taking a look at all the available methods e.g.
    on <Link href="https://devdocs.io/javascript-array/">devdocs.io</Link>.
  </Text>,
];

<KnowledgePill {...props} {...props.frontmatter}>
  <Title level={4} transform="none">
    Explore ECMAScript
  </Title>
  <SyntaxHighlighter>{codeSnippet}</SyntaxHighlighter>
  <Title level={4} transform="none">
    Explanation
  </Title>
  <PostList data={listData} />
</KnowledgePill>
